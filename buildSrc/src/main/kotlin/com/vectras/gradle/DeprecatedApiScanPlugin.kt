package com.vectras.gradle

import org.gradle.api.Plugin
import org.gradle.api.Project

// Intentionally empty Kotlin stub to avoid classpath conflicts; real implementation in Groovy.
class DeprecatedApiScanPlugin : Plugin<Project> {
    override fun apply(project: Project) { /* no-op stub */ }
}
