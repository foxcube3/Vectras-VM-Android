# Git LFS tracking for large binary/runtime assets
# QEMU firmware / vars images
app/src/main/assets/roms/*.img filter=lfs diff=lfs merge=lfs -text

# (Removed .so LFS tracking; native libs now built from source via CMake)

# (Optional) future binary patterns can be added below
# Example:
# app/src/main/assets/roms/*.bin filter=lfs diff=lfs merge=lfs -text